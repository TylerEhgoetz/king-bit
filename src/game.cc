#include "game.h"

void Game::placePiece(int piece, int square) {
    // TODO: call position placePiece once created
}

void Game::removePiece(int square) {
    // TODO: call position removePiece once created
}

void Game::setColour(int colour) {
    // TODO: make it colours turn to play (have to set up main first)
}
